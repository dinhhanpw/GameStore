<Window x:Class="GameStore.Dialogs.CaroGameConfigDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GameStore.Dialogs"
        mc:Ignorable="d"
        Title="CaroGameConfigDialog" Height="150" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Margin="10" Text="Size of Board (6-10):" HorizontalAlignment ="Right"/>
        <TextBox Margin="10" Name="sizeTextBox" Grid.Column="1" />
        <StackPanel Grid.Row="1" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top">
            <Button Margin="10" HorizontalAlignment="Right" Name="okButton" Width="50" Content="Ok" IsDefault="True" Click="okButton_Click"/>
            <Button Margin="10" HorizontalAlignment="Right" Name="cancelButton" Width="50" Content="Cancel" IsCancel="True"/>

        </StackPanel>
    </Grid>
</Window>
